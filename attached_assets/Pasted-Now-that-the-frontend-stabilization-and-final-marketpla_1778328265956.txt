Now that the frontend stabilization and final marketplace flow are completed, I need you to generate the FINAL Supabase SQL that matches the CURRENT FINAL CODEBASE exactly.

IMPORTANT:
Do NOT generate generic SQL.
Do NOT generate placeholder schema.
The SQL must be based on the ACTUAL FINAL FRONTEND CODE and ACTUAL DATA FLOW currently used in the project.

Your task:

1. Inspect the FINAL current codebase carefully
   Audit:

* services
* hooks
* Supabase queries
* inserts
* updates
* realtime subscriptions
* auth flow
* admin flow
* seller flow
* buyer flow
* payment flow
* withdrawal flow
* notification flow
* wallet flow
* cart flow
* chat flow

2. Generate ONE FINAL SQL schema/update that is fully compatible with the CURRENT FINAL PROJECT.

The SQL must include:

* all required tables
* all required columns
* all required indexes
* all required realtime publication setup
* all required functions/RPC
* all required triggers
* all required default settings data
* all required constraints
* all required relationships
* all required wallet/payment/order logic support

3. Verify compatibility with the frontend
   Before finalizing SQL:

* compare every Supabase query in frontend
* verify no missing columns
* verify no missing tables
* verify no missing RPC/functions
* verify no mismatched field names
* verify all role-based queries work

4. Verify all marketplace flows with the SQL:

* buyer register/login
* seller register/login
* seller approval
* seller upload product
* checkout
* payment proof upload
* admin payment approval
* order status updates
* chat realtime
* notifications realtime
* withdrawal request
* withdrawal approval/rejection
* wallet updates
* cart persistence
* dashboard refresh persistence
* realtime subscriptions

5. RLS POLICY STRATEGY
   For now use SIMPLE STABLE mode:

* avoid enterprise-level strict policies
* do not block working frontend flows
* prioritize stable marketplace functionality
* keep RLS simple and compatible with current frontend logic

6. Realtime
   Ensure SQL enables realtime correctly for:

* users
* products
* orders
* chats
* chat_messages
* notifications
* withdrawals
* seller_wallets
* payments
* carts
* user_presence
* reviews
* seller_applications
* seller_verifications

7. Auth support
   Ensure:

* auth.users auto-creates profile in users table
* role system works
* seller approval works
* sessions survive refresh
* dashboards load after refresh

8. Final verification
   After generating SQL:

* verify npm run build still passes
* verify no frontend query breaks
* verify no missing schema
* verify no missing data flow
* verify no missing realtime table

9. Final output required
   Provide:
10. FINAL SQL ready to paste once into Supabase SQL Editor
11. List of tables created
12. List of functions/RPC created
13. List of realtime-enabled tables
14. Any ENV variables required
15. Any migration notes if needed
16. Confirmation that the SQL matches the final stabilized frontend exactly

IMPORTANT:
The goal is:
FULLY STABLE + FULLY CONNECTED + READY TO PUBLISH.

Do not redesign the app.
Do not add new features.
Do not simplify existing marketplace logic.
Only generate the correct final SQL for the stabilized final marketplace system.
