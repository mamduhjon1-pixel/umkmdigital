I have an existing website project and I will upload the ZIP/project files to you.

Your task is NOT to rebuild the website from scratch.
Your task is to CLEAN, STABILIZE, and PREPARE the EXISTING PROJECT so that:

1. It works correctly in Replit
2. It can be pushed to GitHub cleanly
3. It deploys to Vercel successfully WITHOUT errors
4. npm install works
5. npm run build works
6. The deployed website works normally after deployment

IMPORTANT:

* Keep the CURRENT UI and features
* Do NOT redesign the website
* Do NOT remove existing marketplace logic
* Do NOT replace Supabase
* Use the uploaded project as the main source
* Keep the current React/Vite + Supabase architecture

Main Goal:
When I push the final project to GitHub and import it into Vercel, deployment should succeed immediately without npm install errors, workspace errors, catalog errors, or build failures.

Please do ALL of the following carefully:

1. PROJECT STRUCTURE AUDIT
   Inspect:

* package.json
* package-lock.json
* vite.config
* tsconfig
* node_modules references
* build scripts
* environment variables
* Replit-specific configs
* workspace dependencies
* monorepo configs
* pnpm/yarn configs
* alias paths
* Supabase configs

2. REMOVE REPLIT/MONOREPO DEPENDENCIES
   If the project contains:

* workspace:*
* catalog:
* Replit monorepo dependencies
* @replit/vite-plugin
* local workspace packages
* unsupported Vercel configs

convert the project into a NORMAL standalone Vite project that works with standard npm install.

3. KEEP THE WEBSITE LOGIC
   Do NOT break:

* admin dashboard
* seller dashboard
* buyer dashboard
* Supabase auth
* realtime features
* chat
* notifications
* checkout
* products
* orders
* payments
* withdrawal
* seller approval
* wallet
* cart

4. DEPLOYMENT COMPATIBILITY
   Prepare the project specifically for:

* GitHub
* Vercel
* React/Vite SPA routing

Add/fix:

* vercel.json
* .env.example
* proper build scripts
* proper output directory
* proper SPA rewrite rules

5. BUILD TESTS
   Before finalizing:
   Run:

* npm install
* npm run dev
* npm run build

Requirements:

* zero npm install errors
* zero workspace errors
* zero catalog errors
* zero missing dependency errors
* zero TypeScript import errors
* zero Vercel deployment errors

6. FINAL PACKAGE REQUIREMENTS
   The final project must:

* work as standalone project
* deploy directly to Vercel
* not require Replit-specific environment
* not require workspace packages
* not require pnpm workspace
* not require monorepo setup

7. ENVIRONMENT VARIABLES
   Ensure project only needs:
   VITE_SUPABASE_URL
   VITE_SUPABASE_PUBLISHABLE_KEY
   VITE_CLOUDINARY_CLOUD_NAME
   VITE_CLOUDINARY_UPLOAD_PRESET

Generate a proper .env.example file.

8. FINAL OUTPUT
   At the end:

* explain what was fixed
* explain what Replit-specific configs were removed
* confirm npm install works
* confirm npm run build works
* confirm Vercel deployment should succeed
* provide final clean project structure

IMPORTANT:
The final project must be:

* stable
* standalone
* GitHub-ready
* Vercel-ready
* production-ready

without changing the existing marketplace UI and features.
